{
  "devDependencies": {
    "gts": "^6.0.1",
    "typescript": "^5.6.3",
    "@types/node": "^22.7.5"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint"
  }
}
